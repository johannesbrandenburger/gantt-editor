Find bugs in the canvas rewrite and fix them. After each bug, do a commit with a short but descriptive commit message, but do not push anything! After each bug, increase the counter below and start compaction so that you don't bloat the context window. Then in the new session pick up another bug and start again without interrupting.
One note: there may be also bugs that came from the original implementation, ported over to the canvas. Do also fix those bugs!

Bugs: 2/20